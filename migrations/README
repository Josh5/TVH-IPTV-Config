# Create migrations
```
sudo docker compose \
    -f ./docker/docker-compose.dev-aio.yml run \
    --user $(id -u) \
    --entrypoint='' tic \
    /var/venv-docker/bin/python -m alembic revision --autogenerate
```
