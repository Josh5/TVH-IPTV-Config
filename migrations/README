# Create migrations
```
docker compose \
    -f docker-compose.yml -f docker-compose.dev-override.yml run \
    --entrypoint='' tvh-iptv-config \
    /var/venv-docker/bin/python -m alembic revision --autogenerate
```
